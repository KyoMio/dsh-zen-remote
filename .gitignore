node_modules/
*.log
.DS_Store
.pnpm-store/
.learnings/
.codegraph/
.client-build/
test/.tmp/
*.tgz

# Local planning note, deliberately not shipped: it records the pre-implementation
# guesses (several of which turned out wrong) and is superseded by README's
# notification section and docs/remote-access.md.
docs/push-policy.md
